import { CoupurechainePipe } from './coupurechaine.pipe';

describe('CoupurechainePipe', () => {
  it('create an instance', () => {
    const pipe = new CoupurechainePipe();
    expect(pipe).toBeTruthy();
  });
});
