import { OperatorImagePipe } from './operator-image.pipe';

describe('OperatorImagePipe', () => {
  it('create an instance', () => {
    const pipe = new OperatorImagePipe();
    expect(pipe).toBeTruthy();
  });
});
